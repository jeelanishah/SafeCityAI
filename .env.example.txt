@"
# SafeCityAI Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git - it contains secrets!

# Application Settings
ENVIRONMENT=development
DEBUG=True
LOG_LEVEL=INFO
SECRET_KEY=change-this-to-a-random-32-char-string-before-production

# Model Configuration
MODEL_TYPE=yolov5
MODEL_SIZE=s
MODEL_WEIGHTS_PATH=./models/weights/best.pt
MODEL_VERSION=1.0.0
MODEL_RUN_NAME=safecityai_v1
CONFIDENCE_THRESHOLD=0.55
IOU_THRESHOLD=0.45
MAX_DETECTIONS=100

# Data Paths
DATA_ROOT_PATH=./data
RAW_DATA_PATH=./data/raw
PROCESSED_DATA_PATH=./data/processed
DATASET_YAML_PATH=./data/data.yaml
ANNOTATIONS_FORMAT=yolo

# Training Configuration
EPOCHS=100
BATCH_SIZE=16
IMAGE_SIZE=640
LEARNING_RATE=0.01
WEIGHT_DECAY=0.0005
PATIENCE=20
NUM_WORKERS=4
DEVICE=auto
SEED=42

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4
API_RELOAD=False
MAX_UPLOAD_SIZE_MB=10
API_TIMEOUT_SECONDS=30
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Roboflow Integration
ROBOFLOW_API_KEY=your_api_key_here
ROBOFLOW_WORKSPACE=your_workspace_slug
ROBOFLOW_PROJECT=traffic-violations
ROBOFLOW_VERSION=1

# Logging Configuration
LOG_FILE_PATH=./logs/safecityai.log
LOG_ROTATION=500 MB
LOG_RETENTION=10 days

# Inference Configuration
INFERENCE_DEVICE=auto
INFERENCE_BATCH_SIZE=8

# Output Paths
OUTPUT_BASE_PATH=./outputs
PREDICTIONS_OUTPUT_PATH=./outputs/predictions
VIDEOS_OUTPUT_PATH=./outputs/videos
REPORTS_OUTPUT_PATH=./outputs/reports
"@ | Out-File -FilePath .env.example -Encoding UTF8